apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: birdnet
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/lancehampton/talos-homelab.git
    path: applications/birdnet
    targetRevision: main
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: birdnet
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
